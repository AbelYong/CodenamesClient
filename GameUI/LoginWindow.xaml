<Window x:Class="CodenamesClient.GameUI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CodenamesClient.GameUI"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:CodenamesClient.Properties.Langs"
        Title="Codenames: Duet" Height="640" Width="1024" Background="#FF1D1D1D">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Height="110" Orientation="Horizontal" HorizontalAlignment="Center">
                <Label Content="Codenames:" Margin="0,0,0,0"  Foreground="White" FontFamily="Consolas" FontSize="72"/>
                <Label Content="Duet" Margin="0,10,0,0" Foreground="White" FontSize="72" FontFamily="Bell MT"/>
            </StackPanel>
            <Label Content="{x:Static language:Lang.globalUsername}" Style="{StaticResource subtitle}" Margin="0,10,0,0"/>
            <TextBox Name="txb" Style="{StaticResource textBoxCustomFocus}" Margin="0,10,0,0" Text="" Width="320" />
            <Label Content="*errorMessage1" Style="{StaticResource normal}" Margin="0,10,0,0" Width="438"/>
            <Label Content="{x:Static language:Lang.loginPassword}" Style="{StaticResource subtitle}" Margin="0,10,0,0"/>
            <PasswordBox Style="{StaticResource passwordBoxCustomFocus}" Margin="0,10,0,0" Width="320"/>
            <Label Content="*errorMessage2" Style="{StaticResource normal}" Margin="0,10,0,0" Width="438"/>
            <Button Content="{x:Static language:Lang.loginLogIn}" Style="{StaticResource buttonCustomFocus}" Margin="0,10,0,0" Width="316" Background="#00DDDDDD" FontSize="24" Height="60"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Label Content="{x:Static language:Lang.loginDontHaveAccount}" Style="{StaticResource normal}" Margin="0,0,0,0"/>
                <Button Content="{x:Static language:Lang.loginSignIn}" Style="{StaticResource buttonCustomFocus}" Margin="0,0,0,0" FontFamily="Consolas" FontSize="16" Background="#00DDDDDD" BorderBrush="#00000000" FontWeight="Normal" FontStyle="Italic" Width="107"/>
            </StackPanel>
            <Button Content="{x:Static language:Lang.loginPlayAsGuest}" Style="{StaticResource buttonCustomFocus}" Margin="0,10,0,0" VerticalAlignment="Top" Height="60" Width="316" Background="Transparent" BorderBrush="White" FontSize="24"/>
        </StackPanel>
    </Grid>
</Window>
